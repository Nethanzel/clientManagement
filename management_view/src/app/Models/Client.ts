export class Client {
    constructor(
        public clientId:Number, 
        public firstName: string,
        public lastName: string,
        public phone?: string,
        public email?: string
    ) {  } 
}